import { CardView, TableView } from '@banx/icons'
import { ViewState } from '@banx/store'

export const buttonConfigurations = [
  { state: ViewState.CARD, icon: <CardView /> },
  { state: ViewState.TABLE, icon: <TableView /> },
]
